meta {
  name: Create Job Board
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/api/job-boards
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  slug: meta8
  logo: @file(Meta-Logo.png)
}

settings {
  encodeUrl: true
  timeout: 0
}
